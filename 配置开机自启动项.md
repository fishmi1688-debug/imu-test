# 用 systemd 最稳，建议直接以 root 启动（你的 start_system.sh 里要配 CAN、蓝牙，普通用户容易卡在权限）

## 0. 蓝牙连接失败排查（Android APP 报 "read failed, ret:-1"）

### 根本原因

1. **BlueZ 未启用 `--compat` 模式** → `sdptool add --channel 1 SP` 返回255，SDP服务记录未注册，Android无法通过UUID发现服务
2. **`BT_SECURITY_LOW` 结构体打包错误** → 原 `struct.pack("BB",...)` 仅2字节，内核`setsockopt`要求4字节对齐，静默失败导致安全级别未降低，未配对设备被内核直接断开（表现为connect成功但立即read返回-1）

### 修复方法（在主控板 root@ubuntu 上执行一次）

```bash
# ① 启用 bluetoothd --compat 模式（让 sdptool 能注册 SDP）
# start_system.sh 已内置自动修复，手动操作如下：
mkdir -p /etc/systemd/system/bluetooth.service.d
tee /etc/systemd/system/bluetooth.service.d/compat.conf > /dev/null << 'EOF'
[Service]
ExecStart=
ExecStart=/usr/lib/bluetooth/bluetoothd --compat
EOF
systemctl daemon-reload
systemctl restart bluetooth

# 验证（应看到 --compat 参数）
pgrep -a bluetoothd

# ② 验证 sdptool 现在可以注册（应输出完成信息，不再返回255）
sdptool add --channel 1 SP
```

> **代码修复（已提交）：** `bluetooth_server.py` 中 `BT_SECURITY_LOW` 结构体由 `struct.pack("BB", 1, 0)` 改为 `struct.pack("BBBB", 1, 0, 0, 0)`（4字节），`start_system.sh` 新增 `ensure_bluetooth_compat()` 函数在每次启动时自动检查并应用上述 systemd override。

---

```bash
scp -r "/media/zhang/新加卷/wc/PC-test/gait_control_ws" root@192.168.43.188:/home/sunrise
scp -r "/media/zhang/新加卷/wc/test/new-dianji/PC-test/gait_control_ws" root@192.168.43.34:/home/sunrise
scp -r "/media/zhang/新加卷/wc/test/imu-test/PC-test/gait_control_ws" root@192.168.43.4:/home/sunrise
 ip link set can0 up type can bitrate 1000000
```

## 1. 先确认手动可启动（只做一次）

```bash
cd /home/sunrise/gait_control_ws
chmod +x start_system.sh setup_env.sh setup_can.sh
sudo GAIT_WORKSPACE=/home/sunrise/gait_control_ws ./start_system.sh
```

## 2. 创建开机自启服务

```bash
sudo tee /etc/systemd/system/gait.service > /dev/null <<'EOF'
[Unit]
Description=Gait Control System (RDK X5)
DefaultDependencies=no
After=local-fs.target systemd-modules-load.service systemd-udevd.service
Before=basic.target multi-user.target
StartLimitIntervalSec=0

[Service]
Type=simple
User=root
WorkingDirectory=/home/sunrise/gait_control_ws
Environment=GAIT_WORKSPACE=/home/sunrise/gait_control_ws
Environment=GAIT_BT_ENABLE=1
Environment=GAIT_BT_PREPARE_BEFORE_MAIN=0
Environment=GAIT_BT_PREPARE_DELAY_SEC=10
Environment=GAIT_BT_SETUP_IN_SERVER=1
Environment=GAIT_CAN_EXIST_TIMEOUT=4
Environment=GAIT_MAIN_READY_CHECK=process
Environment=GAIT_MAIN_START_CONFIRM_SEC=0.1
Environment=GAIT_FAST_ROS_ENV=1
Environment=GAIT_PYTHON_BIN=/usr/bin/python3
Environment=GAIT_BOOT_IMU_PHASE_RATE_CHECK_SEC=0.3
Environment=ROS_LOCALHOST_ONLY=1
ExecStart=/bin/bash /home/sunrise/gait_control_ws/start_system.sh
Restart=always
RestartSec=1
KillMode=control-group
TimeoutStopSec=15

[Install]
WantedBy=basic.target
EOF
```

> 如果之前已经创建过 `gait.service`，先执行 `sudo systemctl disable gait.service`，再重新执行上面的 `sudo tee ... EOF` 覆盖旧配置。这个早启动版本挂到 `basic.target`，并删除 `Wants=network-online.target`、`After=network-online.target` 和 `After=bluetooth.service`，默认助力不再等待网络、蓝牙或普通多用户阶段完成。

## 3. 生效并启动

```bash
sudo systemctl daemon-reload
sudo systemctl reenable gait.service
sudo systemctl restart gait.service
```

## 4. 查看状态和日志

```bash
systemctl status gait.service
journalctl -u gait.service -f
```

## 5. 关闭自启（如需）

```bash
sudo systemctl disable --now gait.service
```

## 6.蓝牙检查

```bash
systemctl status gait.service --no-pager -l
journalctl -u gait.service -n 120 --no-pager | grep -E "蓝牙|RFCOMM|启动失败|SDP|discoverable|pairable"
bluetoothctl show | grep -E "Name|Alias|Powered|Discoverable|Pairable"
```

```bash
sudo rfkill unblock bluetooth
sudo systemctl restart bluetooth
bluetoothctl power on
bluetoothctl discoverable on
bluetoothctl pairable on
bluetoothctl show | grep -E "Powered|Discoverable|Pairable|Name|Alias"
```
